#include "main.h"


/**
 * add - a function that adds two numbers and returns result
 * @x: first number input
 * @y: second number inpur
 *
 * Return: x + y
 */
int add(int x, int y)
{
	return (x + y);
}
